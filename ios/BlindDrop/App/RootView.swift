import SwiftUI

/// Placeholder. `E08-01` replaces this with the composition root, and `docs/13` §4 defines
/// the real routing: signedOut → noProfile → noGroup → ready.
///
/// Deliberately empty of copy: every user-facing string comes from
/// `Resources/Localizable.strings` (`docs/11`), and there is nothing to say yet.
struct RootView: View {
    var body: some View {
        Color.clear
    }
}
