import SwiftUI

/// Placeholder. E09-01 builds Sign in with Apple (`docs/08` §1.1).
///
/// Deliberately empty of copy: every user-facing string comes from
/// `Resources/Localizable.strings` (`docs/11`), and inventing one here to fill a placeholder
/// is how strings get written outside the copy deck.
struct SignInScreen: View {
    var body: some View {
        Color.clear
    }
}
