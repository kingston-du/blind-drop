import SwiftUI

/// Placeholder. E09-02 builds the display-name step (`docs/08` §1.2), reached when the server
/// answers `NO_PROFILE` (`docs/04` §2).
struct DisplayNameScreen: View {
    var body: some View {
        Color.clear
    }
}
