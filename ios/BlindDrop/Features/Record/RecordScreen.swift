import SwiftUI

/// Placeholder. E13-01 builds The Record (`docs/08` §8). Pushed, always reachable — one of the
/// two typed destinations in `Route`.
struct RecordScreen: View {
    var body: some View {
        Color.clear
    }
}
